# CS posting selection and NCS loading: execution record

This record is pinned to READY JOB-ALIO snapshot `4eae07ed-1b70-4146-8e4f-75291c6b6b13`
(530 source postings) and NCS snapshot `a32170ed-7485-4e31-82d3-ed48b3398946`.
The scope policy is `cs-it-ai-data-v1`. Selection is per advertised position;
experience and employer eligibility are recorded separately from technical job
relevance. Source-qualified identities such as `job-alio:posting:304958` preserve
ALIO lineage and prevent collisions with later sources. The adapter and collision
contract are in `hop/cs/`.

The next bounded pass source-screened 62 previously unextracted notices. It
confirmed two CS notices, `304933` (one information-security vacancy) and
`304922` (two hires in one IT position), and excluded six using source-specific
evidence. Ten still need roster/JD review; 44 are provisional non-CS screens.
The two confirmed notices were archived, parsed, frozen into exact input
bundles and accepted as source-bound position extractions. One paid Luna
extraction request per notice reported $0.0284467 and $0.0184406; the first
omitted the advertised security vacancy and was corrected from its saved
response and own JD without a second call. A no-cost manual NCS packet accepted
two exact-duty security links and two exact-duty IT-support links. Its receipt
and evidence are in [new-posting triage](cs-new-postings-triage-20260915.md).
The security units have a related C325 qualification and three loaded exam
sessions; the IT-support units have no fetched qualification mapping. Related
means a reference join through NCS, not an employer requirement.
Together with the earlier $0.14888355 pilot, this rollout reported
**$0.19577085** in new model calls. Cached-response review and manual NCS
decisions added no paid calls.

The final reviewed graph checkpoint is `cs-reviewed-20260916-final-b` READY
at 2026-09-15 15:20:43 UTC, with **145 accepted extractions and 385 accepted
NCS links** across all published postings. No-cost review accepted cached,
source-bound extractions for `304387/p1`, `304387/p2` and `304687/p1`.
For `304606/p7`, its overbroad 40-role parent stayed rejected; a p7-only
source-exact child was accepted and its scope decision rebound. These roles,
plus `304432/p3`, are technical under the policy even where eligibility is
senior. The pinned full-catalogue
[five-role audit](cs-ncs-five-role-audit-20260916.md) supported one DBMS
implementation unit for the corrected `304606/p7` duty D3. Native manual
review receipt `f45ed1f72ddc043e5be396a846330900ec258c1ac95cfd0f637daa16a1a83710`
recorded that reviewer-origin candidate and ACCEPT decision; final graph
readback verified its evidence and origin. The audit abstains for the other
four roles. A link coverage target does not justify asserting work the
employer did not describe.

The final read-only `cs.role_completion` check counted **66 selected positions
across 45 ALIO postings**, 54 with accepted position-bound NCS links and 12
without. Nine other roles remain `NEEDS_REVIEW`; 93 are explicit exclusions
and 1,754 are cheap-screen exclusion candidates. The 12 link gaps are
`304387/p1,p2`, `304432/p3`, `304474/p7`, five KISA `304632` tracks
(`p3n02,p3n12,p4s02,p4s03,p4s04`), `304687/p1`, and `304697/p8,p12`.
They are evidence/catalogue gaps, not failed graph writes or paid-call
rejections. `304606/p7` now has one accepted DBMS unit; its exact qualification
summary reports one accepted link, zero related credentials and one
`FETCHED_NO_MAPPING` link. The nine unresolved scope cases and 10 still
uncertain new-notice source screens are the explicit review tail. Do not
equate this tail with 530 fully enriched postings: the 530 are fetched source
records, while publication contains only independently accepted derivatives.

At the initial 2026-09-15 live check, **59 positions across 40 ALIO postings** were
reviewed `IN_SCOPE`. Forty-seven had at least one accepted category-20
(information/communication) NCS link. Four had accepted domain NCS links only;
these are substantive mixed roles, not evidence of a category-20 competency.
Eight selected positions had no supported position-bound NCS link. This is an
assessed selection, not an exhaustive classification of all source roles: 19
positions still had `NEEDS_REVIEW`, and the cheap screen left 1,791
`OUT_OF_SCOPE_CANDIDATE` positions. The initial genuinely new extraction queue
contained 66 postings; four were processed in the bounded pilot. Check the
current views after every source or review refresh because these counts change.

The unresolved eight are `304474/p7` (dementia-policy indicators),
`304697/p8` (monitoring and user complaints), `304697/p12` (linked biobank data
operations), and five separately advertised KISA youth-technical tracks
`304632/p3n02`, `p3n12`, `p4s02`, `p4s03`, `p4s04`. Their available duties are too
sparse to assert the complete current NCS units inspected. The KISA correction
split the source's 30 selectable tracks, then a source-bound packet recorded five
technical selections, nine policy/evidence uncertainties and sixteen exclusions.
The five selected tracks remain explicit evidence/catalogue gaps; a similarly
named security or AI unit must not be added just to reach a coverage target.
See [the detailed NCS gap audit](cs-ncs-gap-audit-20260915.md) and
[false-negative screen audit](cs-false-negative-audit-20260915.md).

The new model work was deliberately bounded. Luna batch
`a3ef3c09-7f78-4a41-aa9d-b39e3d7c09a5` sent six requests for three compact
notices and reported **$0.08913940**. LH batch
`0cc3ce5d-36eb-4541-b781-d934bf8b284c` sent one request and reported
**$0.05974415**; its whole-notice output was rejected for a literal fragment
 and its 17-role roster was not approved. That pilot's reported model cost was
**$0.14888355**. Saved responses and exact A/C notice/JD documents supplied
source-bound child revisions without paid retries. `304713`'s 20 vacancy rows,
KISA's selectable tracks, `304956`'s separate 4급/5급 records vacancies,
`304963`'s `통계` specialty, and LH's **two-hire 5급 전산 role only** were corrected
and independently accepted. LH's other 233 advertised hires remain source-loaded;
the selected-role derivative does not claim a complete extraction of them.
The pilot source and review receipts are in
[new-posting triage](cs-new-postings-triage-20260915.md).

No-cost manual NCS review added four duty-bound links for corrected `304713`
(`809071c49abfdfe275fe876b36b9cc17ce2d752b0be432ab75a6a9d3ec0e9e66`),
three for corrected `304956`/`304963`
(`91532ba8d84390e8a6d5af692c2666b6e7085ce5e86c11ea5a87e95f06b6c90f`),
and two for LH `304958/p7`
(`d9a057ff9d1cad800769506d79680bf675a0067a9cc6cb32b74645fd090d3494`).
These receipts are immutable packet imports, not graph checkpoints. The
previously published projection remained active when a review changed during
an earlier publication; the guard correctly refused to activate the changing
snapshot. After reviews settled, native `llm/publish_links.hwf` completed
publication **`cs-reviewed-20260915-stable-a` READY** at 14:36:05 UTC. It
froze, loaded and read back **140 accepted posting extractions and 380 accepted
NCS links** in Neo4j. This publication covers all currently accepted postings,
not just the CS subset; use `cs.current_scope`/`cs.accepted_role_link` to select
the CS positions and their exact role duties.

The expanded NCS→qualification reference run
`1cf7e9e1-1eb9-4978-9335-ccd4a7f8c729` is READY with 281 unit partitions,
150 mappings and 48 distinct credential codes. Its dependent Q-Net run
`66aa4bd0-5fca-4fd0-b92f-562a346938e3` is READY with 85 exam sessions.
Use the native `cs/read_qualification.hwf` or
`cs/read_qualification_summary.hwf` with an **exact** `POSTING_IDENTITY` to
inspect an accepted CS role's current mapping and exam status. For example,
`304686/p1` has five related credentials with exam sessions; `304964/p1` has
one related statistical credential whose 2026–27 exam session is not loaded.
The path means *related through NCS*, not an employer-required certificate.
Cloud platform `20020110` and data architecture `20010211` were linked after
the pinned reference refresh and still show `UNFETCHED_QUALIFICATION_PARTITION`.
Add their 19 units to the next qualification-policy window, then refresh Q-Net
only if new credential codes appear. The deployment and status semantics are in
[the qualification record](cs-qualification-deployment-20260915.md).

Remaining no-cost follow-up is to review unresolved source A/C rosters,
including `304913` and `304942`; run model extraction only for a
source-confirmed CS vacancy without a reusable correct response. The 19
`NEEDS_REVIEW` roles and eight NCS gaps above are initial baseline counts;
re-query live views after the current review and publication. Gaps need source
or policy evidence, not an automatic model retry. A future source maps
its native posting ID and evidence into `cs.common_posting`; its actual fetch,
enrichment and graph adapters still need to be built. Source refresh checks
remain on the existing 15-minute host trigger; paid enrichment and CS review
are manual in this phase.

The disposable CS contract checks pass for common-source identities, scope
completion, scope packets, manual NCS links and qualification policy. Hop's
native review imports are append-only with frozen source/revision/catalogue
bindings. Review the current state with `cs/read_scope.hwf`, `cs/assess.hwf`
and exact-identity qualification readers before any new publication.
